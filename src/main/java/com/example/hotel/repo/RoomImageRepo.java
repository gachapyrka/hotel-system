package com.example.hotel.repo;

import com.example.hotel.domain.RoomImage;
import org.springframework.data.repository.CrudRepository;

public interface RoomImageRepo extends CrudRepository<RoomImage, Long> {
}
